{
  "name": "cv-page",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev -H 0.0.0.0 -p 6070",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "predeploy": "next build",
    "deploy": "next export && touch out/.nojekyll && git add out && git commit -m 'Deploy' && git push origin `git subtree split out main`:gh-pages --force"
  },
  "dependencies": {
    "next": "14.2.15",
    "react": "^18",
    "react-dom": "^18",
    "sass": "^1.79.5"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "eslint": "^8",
    "eslint-config-next": "14.2.15",
    "postcss": "^8",
    "tailwindcss": "^3.4.1",
    "typescript": "^5"
  }
}